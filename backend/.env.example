PORT=5000
JWT_SECRET=change-this-secret

# Option 1: URL unica (recomendado en cloud)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname

# Option 2: variables separadas
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=sportcommunity_db

# true para proveedores cloud (Neon, Render, Railway, Supabase)
DB_SSL=false

# Frontend origin permitido (opcional)
# CORS_ORIGIN=https://training-social-media.vercel.app
